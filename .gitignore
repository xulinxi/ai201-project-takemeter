# Raw Reddit captures — large (~480MB of HTML/PDF) and regenerable into candidates.csv.
# Keep a local/backup copy; not version-controlled.
data/raw/

# Intermediate parsed pool — regenerable with `python3 scripts/parse_html.py`
data/candidates.csv

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS / editor
.DS_Store
.ipynb_checkpoints/

scripts/
notebook/